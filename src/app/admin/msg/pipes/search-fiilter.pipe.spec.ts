import { SearchFiilterPipe } from './search-fiilter.pipe';

describe('SearchFiilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFiilterPipe();
    expect(pipe).toBeTruthy();
  });
});
